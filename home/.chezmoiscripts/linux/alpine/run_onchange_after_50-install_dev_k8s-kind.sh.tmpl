#!/usr/bin/env sh

{{ if .is.devtoolsK8s -}}
# dotfiles / devtools / k8s: enabled
{{- end }}

curl -sLO https://github.com/kubernetes-sigs/kind/releases/download/v0.23.0/kind-linux-amd64 \
  && mv ./kind-linux-amd64 ~/.local/bin/kind \
  && chmod +x ~/.local/bin/kind
